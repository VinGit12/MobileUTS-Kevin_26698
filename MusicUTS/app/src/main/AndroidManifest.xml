<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.umn.musicuts">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicUTS">
        <activity android:name=".MusicApp.PlayerActivity"></activity>
        <activity android:name=".MusicApp.MainMusicActivity" />
        <activity android:name=".Profile.ProfilActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="MyMusic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>